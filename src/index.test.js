import { Bar, Line, Pie } from '.';

describe('Bar chart is truthy', () => {
  it('is truthy', () => {
    expect(Bar).toBeTruthy();
  });
});

describe('Line chart is truthy', () => {
  it('is truthy', () => {
    expect(Line).toBeTruthy();
  });
});

describe('Pie chart is truthy', () => {
  it('is truthy', () => {
    expect(Pie).toBeTruthy();
  });
});
